# Grafana Dashboard Provisioning Configuration

apiVersion: 1

providers:
  - name: 'NetVault Dashboards'
    orgId: 1
    folder: 'NetVault'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
