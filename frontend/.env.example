# NetVault Frontend Configuration

# API Base URL (relative path for production, full URL for dev)
REACT_APP_API_URL=/api/v1

# Development server host (only used in dev mode with npm start)
HOST=0.0.0.0
